
      <script type="application/ld+json">
      {
        "@context": "http://schema.org/",
        "@type": "{{ type|default('TechArticle') }}",
        "name" : "{{ name|default('No name') }}",
        "author": {
		  "@type" : "Person",
		  "name" : "Ikaros Kappler"
		  },
        "image": "{{ image|default('https://www.int2byte.de/img/preview-image.png') }}",
        "description": "{{ description|default('') }}",
        "proficiencyLevel" : "{{ proficiencyLevel|default('Begnner') }}",
	"headline" : "{{ headline|default('') }}"
      }
      </script>